# Layout Fragment

> **Not shipped — this is repo-side fragment documentation.**
> `.template.config/template.json` sources nothing from `template/fragments/layout/`, so no generated
> product ever receives this file. The guidance a product actually gets is
> [`template/product-skills/fs-gg-layout/SKILL.md`](../../../template/product-skills/fs-gg-layout/SKILL.md) — the capability's `supplied-by`.
> Recorded as `materializes: none` on the `layout` row of `template/capabilities.yml`, and held there
> by R-FRAG in `tests/Package.Tests/SkillPackageReachTests.fs` (#510).

Adds layout package references and layout guidance.
