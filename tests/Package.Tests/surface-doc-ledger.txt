# S-DOC ledger — public api-surface entries a product skill does NOT name (#507)
#
# WHAT THIS IS. `template/base/docs/api-surface/**` is the signature set a generated product is GIVEN and
# which docs/scaffold-map.md designates authoritative. `template/product-skills/**` is what the product is
# TOLD. Nothing held the two against each other in EITHER direction, so the gap between them grew in
# silence: 242 of 428 public `val`s were named in no product skill at all — and among them were the ONLY
# instruments for the responsiveness evidence those same skills MANDATE (#507, epic FS-GG/.github#423).
#
# SurfaceDocCoverageTests (S-DOC) now holds them together: every public `val` in the shipped api-surface is
# either NAMED in a product skill, or listed HERE. This file is the declared half. Curation is legitimate —
# not every value wants a paragraph — but it must be a DECISION SOMEBODY MADE rather than an omission nobody
# noticed, and that distinction is the entire issue.
#
# THE LEDGER CANNOT ROT. S-DOC fails three ways, not one:
#   * a public val neither documented nor listed here    -> the gap grew, and silently. The #507 shape.
#   * a listed val a skill NOW documents                 -> stale exemption. Delete the line.
#   * a listed val the api-surface no longer ships       -> stale exemption. Delete the line.
# So the ledger can only shrink by documenting, and it cannot outlive its subjects. Without the last two, the
# cheapest response to a growing gap would be to append here, and this file would become the record of the
# problem instead of its fix.
#
# DOCUMENTED MEANS CITED AS CODE (#654). A skill documents a surface when it names it inside a fenced block
# or an inline code span — not when its prose happens to use the word. `isDocumented` used to match the bare
# name anywhere in the skills' text, which credits any public val whose name is also an ordinary English word
# to any sentence that uses the word, about anything: FS.GG.Game#240 wrote "the block above can bind all four
# the same way" — about a fenced code block — and S-DOC concluded a product skill documents the RichText
# combinator `block`. 32 surfaces passed this gate by homonym, 31 of them in no ledger line at all, so for
# them S-DOC was vacuous. They are all declared below, and the guard now requires a citation.
#
# FORMAT:  <category>  <name>
#
# Categories split into PENDING and TERMINAL — the distinction the anti-rot rules turn on. Every category
# satisfies coverage (rule 1): a declared line is a decision somebody made. They differ in what the ledger
# PROMISES about the line's future. `tracked` is PENDING — it retires the day a product skill HERE documents
# it. The other three are TERMINAL — the surface is settled, so no local skill is EXPECTED to document it and
# the line stays until the api-surface stops shipping it (rule 3).
#
#   tracked          A surface a product author would WANT and cannot find. A real gap, tracked by #507 / epic
#                    FS-GG/.github#423 — listed, NOT blessed. These are the ones to write next. PENDING.
#   vocabulary       A constructor or combinator in a type's own vocabulary (a scene shape, a colour, a design
#                    token, a geometry helper). A product composes it straight from the `.fsi`, and a skill that
#                    listed them would be a worse copy of the signature file. TERMINAL.
#   owner-sourced    Documented by an owner-sourced skill (`fs-gg-game-core` / `fs-gg-audio` /
#                    `fs-gg-persistence`) whose local copy ADR-0063 (#965) RETIRED from this provider — the
#                    body still reaches every game/sample-pack product, it is just no longer checked out here,
#                    so S-DOC (which reads only `template/product-skills/**`) can no longer see it. TERMINAL.
#                    See the section note at the foot of this file.
#   owner-documented Documented in FS.GG.Game's `fs-gg-game-core` skill BODY (canonical in FS.GG.Game,
#                    `FS.GG.Game.Skills`, ADR-0063; FS.GG.Game#466/#472), for a surface VENDORED into this
#                    mirror (#984) that no local skill copy ever carried. The citations live in a corpus this
#                    repo cannot read, so no local skill can ever cite these vals: the line is PERMANENTLY
#                    satisfied by cross-repo owner documentation — never pending, never stale by a local write,
#                    terminal until #984 un-vendors the surface. Adjudicated as option 3 of #998
#                    (@ehotwagner, 2026-07-22). TERMINAL. See the section note above the rows.
#
# The ledger keys on the NAME, so one line covers every `.fsi` that declares it. The `# ---- <file>` headers
# are for readers; names carried by more than one signature file are gathered at the foot of this file, since
# no single header is the honest home for them.
#
# `vocabulary` is a CLAIM, and it is deliberately visible so it can be challenged. If one of these deserves
# prose, move it: delete the line and write the paragraph. Down is the intended direction of travel.
#
# Documenting a surface is constrained by R-REACH (#430): a skill may only name APIs every profile it
# materializes on actually receives. Find the skill whose profile gate matches the package's reach — not the
# one whose subject matter feels closest.


# ---- Audio.Elmish/Elmish.fsi
vocabulary  ofEffects
vocabulary  ofEngine

# ---- Audio.Engine/Engine.fsi
vocabulary  createWith
vocabulary  crossFade
vocabulary  defaultSpatial
vocabulary  fadeBus
vocabulary  setListener

# ---- Audio.Host/Host.fsi
vocabulary  panToPosition
vocabulary  tryParse

# ---- Canvas/Elements.fsi
vocabulary  cached
vocabulary  polyline

# ---- Canvas/Persistence.fsi
vocabulary  clampVersion

# ---- Game.Harness/Journey.fsi
# The opaque receipt accessors are the vocabulary for inspecting a runner-issued proof. The producer's
# fs-gg-playtest skill teaches Journey.runScriptWithIdentity at the game-only package reach; citing it
# from this provider's all-profile fs-gg-testing skill would violate R-REACH.
vocabulary  compositionAuthority
vocabulary  initialFingerprintDigest
vocabulary  inputDigest
vocabulary  inputIdentity
vocabulary  inputKind
vocabulary  maxSteps
vocabulary  routeId
owner-documented  runScriptWithIdentity
vocabulary  runnerIdentity
vocabulary  runnerVersion
vocabulary  scenarioId
vocabulary  schemaVersion
vocabulary  scriptDigest
vocabulary  terminalFingerprintDigest
vocabulary  terminalPredicateIdentity
vocabulary  terminalPredicateReached
vocabulary  testId
vocabulary  traceDigest

# ---- Controls.Elmish/ControlsElmish.fsi
vocabulary  adapterDiagnosticToRuntimeDiagnostic
vocabulary  diagnostic
vocabulary  diagnosticsDisabledCompatibility
tracked     interpretControlEffect
tracked     interpretOverlayEffect
vocabulary  interpretOverlayOutcome
vocabulary  interpretPointerEffect
tracked     interpretPointerOutcome
vocabulary  ofMessage
# `program` — WRITTEN (#663). The Elmish front door is now cited in code by fs-gg-elmish, which also records
# what the .fsi cannot: nothing in the shipped surface CONSUMES an AdapterProgram, so an author must not go
# looking for a `run(program)`. Its wrapper `programOfWidget` is still a gap.
tracked     programOfWidget
tracked     runInteractiveAppWithWindowBehavior
vocabulary  runScriptWithAudio
vocabulary  runScriptWithWindowBehavior
vocabulary  runScriptWithWindowBehaviorAndAudio
vocabulary  toCmd
tracked     widgetView

# ---- Controls/Accessibility.fsi
vocabulary  contrast
vocabulary  defaultFor
vocabulary  metadata

# ---- Controls/Attributes.fsi
vocabulary  accessibility
vocabulary  enabled
vocabulary  loading
vocabulary  margin
vocabulary  onWith
vocabulary  orientation
vocabulary  padding
vocabulary  readOnly
vocabulary  standardAttribute
vocabulary  standardEvent
# `Attr.visible` (`value: bool -> Attr<'msg>`) — a state builder beside `enabled` / `readOnly` / `loading`
# directly above, and vocabulary for the same reason they are. It gets a line only NOW because until #692 it
# did not need one: it was CREDITED AS DOCUMENTED by `fs-gg-game-core`'s `let visible : Rect = …` — a local
# rectangle, in a culling example, in a different package, in a skill about game simulation. The same-language
# homonym was not merely deleting blessed lines; it was HIDING a surface that had never had a decision made
# about it. This is that decision.
vocabulary  visible

# ---- Controls/Buttons.fsi
vocabulary  defaults

# ---- Controls/Catalog.fsi
vocabulary  categories
vocabulary  knownControlKinds
vocabulary  markdownSummary
tracked     requiredAttributes
tracked     standardSchema
vocabulary  supportedAttributes
tracked     supportedControls
vocabulary  supportedCount
tracked     supportedEvents
tracked     validateStandardControl

# ---- Controls/Control.fsi
# `checked'` was invisible to S-DOC until #654: the name pattern stopped at the prime, so this public val was
# never extracted, never credited, never ledgered — it passed the gate by being unparseable. An attribute
# combinator, beside `enabled` / `readOnly` / `selected`.
vocabulary  checked'
vocabulary  count
vocabulary  hitTest
vocabulary  icon
vocabulary  lowerCustom
vocabulary  lowerStandard
vocabulary  nearestAuthored
vocabulary  onClickWith
vocabulary  onSelected
# The sibling constructors `create` and `customControl` ARE cited in code by a skill; `standard` is the typed
# `StandardControlKind` variant of the same one-liner, composed straight from the `.fsi`.
vocabulary  standard
tracked     withKey

# ---- Controls/ControlRuntime.fsi
tracked     deriveVisualState

# ---- Controls/CustomControlWidget.fsi
vocabulary  ofControl

# ---- Controls/DataGrid.fsi
vocabulary  columns
vocabulary  focusedCell
vocabulary  selectedRows

# ---- Controls/Diagnostics.fsi
vocabulary  catalogOmission
vocabulary  customExtension
vocabulary  dependencyLeak
vocabulary  duplicateAttribute
vocabulary  duplicateRuntimeDefinition
vocabulary  keyCollision
vocabulary  missingAccessibility
vocabulary  missingRequired
vocabulary  missingStandardAttribute
tracked     stabilityReport
vocabulary  staleEventTarget
vocabulary  stalePackageReference
vocabulary  unsupportedEnvironment
vocabulary  unsupportedScopeExpansion
vocabulary  unsupportedStandardAttribute
vocabulary  unsupportedStandardEvent

# ---- Controls/Pointer.fsi
vocabulary  origin
vocabulary  toMsg

# ---- Controls/RichText.fsi
# Restored by #654. #649 deleted this line under protest: no skill documented `block`, but FS.GG.Game#240
# wrote "the block above can bind all four the same way" — about a fenced code block — into the frozen-mirror
# fs-gg-audio canonical, and the old prose-matching `isDocumented` read that as documentation and called this
# line stale. `isDocumented` now requires a code citation, so the homonym no longer counts and the line stands.
vocabulary  block
vocabulary  defaultStyle

# ---- DesignSystem/DesignTokens.fsi
vocabulary  background
vocabulary  contrastRequiredRatio
vocabulary  controlHeight
vocabulary  controlHeightLg
vocabulary  controlHeightSm
vocabulary  cornerRadius
vocabulary  danger
vocabulary  density
vocabulary  fontFamily
vocabulary  fontSize
vocabulary  foreground
vocabulary  muted
vocabulary  spaceLg
vocabulary  spaceMd
vocabulary  spaceSm
vocabulary  spaceXs
vocabulary  success
vocabulary  warning

# ---- DesignSystem/Types.DesignSystem.fsi
vocabulary  neutral

# #984 — VENDORED BY #984, DOCUMENTED NOWHERE YET. The game-profile simulation modules
# (Ai/Difficulty, Ballistics, Dice, Effects, Fov, Los, Visibility, Scene.Animation) were entirely
# WAIVED out of the mirror; #984 un-waived them so a game scaffold carries the full surface. Their
# vals were documented in NO shipped skill and NOT in the owner-sourced `fs-gg-game-core` (which covers
# only fixed-step / RNG / pathfinding / spatial-grid / geometry / physics — NOT these), so they were
# `tracked` gaps here: a game author WANTS them and cannot find them. Each line is documented in the
# reach-matching skill, at which point S-DOC rule 2 takes it the moment its skill cites it. Filed as
# FS-GG/FS.GG.Rendering follow-up #986 to #984.
#
# SPLIT — #986 (2026-07-22). The **Scene.Animation** dozen is now documented in the shipped `fs-gg-scene`
# skill (a game/base-profile skill whose reach carries the Scene package), so its rows are GONE below — see
# the `# ---- Scene/Animation.fsi` note. The **Game.Core** half (Ai/Difficulty, Ballistics, Dice, Effects,
# Fov, Los — 40 vals) split again by REACH: Los/Fov map to the Rendering-owned `fs-gg-line-drawing` /
# `fs-gg-visibility` skills (editable HERE), the other 37 to the owner-sourced `fs-gg-game-core` body.
#
# LOS/FOV DONE — #993 (2026-07-22). The 3 R-REACH-Rendering vals — `fov` -> `fs-gg-visibility`,
# `lineOfSightBy` / `trace` -> `fs-gg-line-drawing` — are now cited AS CODE by those skills, so their rows
# are GONE below (see the `# ---- Game.Core/Fov.fsi` and `# ---- Game.Core/Los.fsi` notes).
#
# THE 37 Ai/Difficulty/Ballistics/Dice/Effects VALS ARE `owner-documented` — a PERMANENT, TERMINAL
# disposition, distinct from the pending `tracked` state they sat in through #984/#993. FS.GG.Game#466/#472
# documented them in the `fs-gg-game-core` body (CANONICAL in FS.GG.Game, `FS.GG.Game.Skills`, ADR-0063) — but
# ADR-0063 (#965) RETIRED the local `fs-gg-game-core` SKILL copy from THIS provider, and S-DOC reads ONLY
# `template/product-skills/**` (SurfaceDocCoverageTests.productSkillsRoot). So the FS.GG.Game#472 citations are
# invisible to S-DOC here: the api-surface for these modules is vendored in (#984, so they are in
# `shippedSurface`), but no skill in this repo's corpus can cite them. #993's Ask 2 assumed the republish would
# surface the citations here; it does not, and retiring these 37 was PROVEN to red rule 1 (all 37 ->
# undeclared).
#
# THE DECISION — #998, option 3, adjudicated by @ehotwagner (2026-07-22). The surface stays VENDORED (respects
# #984/#644) and the docs stay in FS.GG.Game's `fs-gg-game-core` skill (respects ADR-0063). Neither vendoring
# the owner corpus into `template/product-skills/` (against ADR-0063) nor un-vendoring the surface (against
# #984) is taken. Instead these 37 are recorded under a PERMANENT ledger category — `owner-documented`: the
# documentation exists, cross-repo, owner-sourced, and this repo's ledger records exactly that. S-DOC accepts
# `owner-documented` as a TERMINAL disposition — a val in it is neither `undeclared` (rule 1: it is declared)
# nor `tracked`-stale (rule 2: no local skill can ever cite it, so the anti-rot rule can never fire against a
# correct line). It retires only if #984 un-vendors the surface (rule 3 drops the .fsi, phantoms the line) or a
# Rendering-owned skill ever documents one of these locally (delete that line). Both keep this section honest.

# ---- Game.Core/Ai.fsi
owner-documented  aimError
owner-documented  best
owner-documented  due
owner-documented  easy
owner-documented  fleeField
owner-documented  ghosts
owner-documented  hard
owner-documented  influenceMap
owner-documented  known
owner-documented  normal
owner-documented  spotted
owner-documented  substream
owner-documented  threatField
owner-documented  tryFind
owner-documented  viewTick

# ---- Game.Core/Ballistics.fsi
owner-documented  intercept
owner-documented  inverseSquareFalloff
owner-documented  linearFalloff
owner-documented  splash

# ---- Game.Core/Dice.fsi
owner-documented  advantage
owner-documented  constant
owner-documented  convolve
owner-documented  die
owner-documented  disadvantage
owner-documented  outcomes
owner-documented  totalWeight
owner-documented  uniform
owner-documented  variance

# ---- Game.Core/Effects.fsi
owner-documented  amplify
owner-documented  applyAll
owner-documented  applyEffect
owner-documented  floorAt
owner-documented  gatedBy
owner-documented  immuneWhen
owner-documented  pipeline
owner-documented  resist
owner-documented  tickEffects

# ---- Game.Core/Fov.fsi   (vendored by #984 — DOCUMENTED by #993; no rows remain here)
# `fov` is now cited AS CODE by the Rendering-owned `fs-gg-visibility` skill's "Grid field-of-view —
# the framework `Fov` module" section (`Fov.fov canSeeThrough eye radius`, qualified). Per #986's
# R-REACH note the Fov surface maps to `fs-gg-visibility` (gated to `game`/`sample-pack`, matching
# `FS.GG.Game.Core`'s reach), NOT the owner-sourced `fs-gg-game-core` — so FS.GG.Game#466/#472 could not
# touch it. S-DOC rule 2 requires the line be GONE.

# ---- Game.Core/Geometry.fsi
vocabulary  obbPolygon
vocabulary  ofCenter
vocabulary  polygonContact
vocabulary  polygonManifold
vocabulary  segmentAabbHit
vocabulary  segmentCircleHit
vocabulary  segmentPolygonHit

# ---- Game.Core/Los.fsi   (vendored by #984 — DOCUMENTED by #993; no rows remain here)
# `line` / `supercover` / `lineOfSight` are carried by other files and were already accounted for — they
# never had rows here. The two that were new — `lineOfSightBy` and `trace` — are now cited AS CODE by the
# Rendering-owned `fs-gg-line-drawing` skill's "The framework `Los` module" section (`Los.trace Thin a b`,
# `Los.lineOfSightBy Supercover isTransparent a b`, qualified). Per #986's R-REACH note the Los surface
# maps to `fs-gg-line-drawing` (gated to `game`/`sample-pack`, matching `FS.GG.Game.Core`'s reach), NOT the
# owner-sourced `fs-gg-game-core` — so FS.GG.Game#466/#472 could not touch it. S-DOC rule 2 requires the
# lines be GONE.

# ---- Game.Core/Pathfinding.fsi
tracked     distanceField
tracked     flowField
vocabulary  reachableWithin

# ---- Game.Core/Rng.fsi
vocabulary  nextBool

# ---- Game.Core/SpatialGrid.fsi
tracked     buildBounds
vocabulary  queryBounds

# ---- Game.Core/MapAnalysis.fsi and Game.Core/MapGen.fsi
# FS.GG.Game 0.9.0 added the map-construction and analysis surface. Every val below is cited AS CODE by
# the owner-published `fs-gg-mapcraft` skill in FS.GG.Game.Skills 0.3.0. ADR-0063 keeps that owner corpus
# out of `template/product-skills/**`, so S-DOC cannot observe those citations locally; this is the same
# terminal cross-repo disposition adjudicated for the Ai/Ballistics/Dice/Effects surface above.
owner-documented  articulationPoints
owner-documented  borderOpenings
owner-documented  bspDungeon
owner-documented  caves
owner-documented  classify
owner-documented  componentCount
owner-documented  connect
owner-documented  coverMap
owner-documented  deadEnds
owner-documented  diameter
owner-documented  exposureMap
owner-documented  fairness
owner-documented  filled
owner-documented  floorLayout
owner-documented  floorSeed
owner-documented  get
owner-documented  heightField
owner-documented  inBounds
owner-documented  isConnected
owner-documented  isolation
owner-documented  killzones
owner-documented  largestRegion
owner-documented  maze
owner-documented  poissonScatter
owner-documented  reachable
owner-documented  regions
owner-documented  set
owner-documented  stranded

# ---- KeyboardInput/KeyboardInput.fsi
vocabulary  noModifiers
vocabulary  normalize
vocabulary  normalizeEvent
vocabulary  normalizeEventWithModifiers
vocabulary  stateDisplay

# ---- Layout/Graph.fsi
vocabulary  directed
vocabulary  undirected

# ---- Layout/GraphValidation.fsi
vocabulary  disconnectedComponents
vocabulary  hasCycle

# ---- Layout/Layout.fsi
vocabulary  dock
tracked     evaluateIncremental
tracked     hitTestComputed
vocabulary  horizontalStack
vocabulary  initWorkflow
vocabulary  interpretWorkflowEffect
vocabulary  measureHorizontal
vocabulary  measureVertical
vocabulary  renderComputed
vocabulary  snapBounds
vocabulary  updateWorkflow
vocabulary  verticalStack

# ---- Layout/Types.fsi
vocabulary  dockConfig
vocabulary  graphConfig
vocabulary  layoutGap
vocabulary  layoutIntent
vocabulary  layoutSize
vocabulary  pixelSnapPolicy
vocabulary  stackConfig

# ---- Scene/Animation.fsi   (vendored by #984 — DOCUMENTED by #986; no rows remain here)
# The twelve `Transform` / `Tween` / `Animation` / `AnimationState` vals #984 left `tracked` here —
# `applyAt`, `identity`, `isActive`, `isIdentity`, `isSettled`, `lerpColor`, `lerpFloat`, `progress`,
# `retarget`, `sampleColor`, `sampleFrames`, `toPerspectiveTransform` — are now cited AS CODE by the
# shipped `fs-gg-scene` skill's "Declarative motion — animation as data" section (qualified: `Transform.identity`,
# `Animation.applyAt`, `AnimationState.retarget`, …), so S-DOC rule 2 requires their lines be GONE, not left to
# imply the surface is still undocumented. (`empty` / `create` / `advance` / `value` / `apply` / `sample` /
# `lerp` were carried by other files and already accounted for — they never had rows here.)

# ---- Scene/Scene.fsi
# `describe` has NO line here, and #692 is the record of why — because its absence has meant two different
# things and only the second one is honest.
#
# It WAS `vocabulary describe` (blessed: a product composes `Scene.describe` straight from the `.fsi`). PR #691
# had to delete it, with `main` red and no cheap alternative: `fs-gg-persistence`'s canonical body — a FROZEN
# MIRROR of FS.GG.Game's, so not editable here — carries an unrelated example helper `let describe (outcome:
# PersistenceOutcome)`, the matcher read that BINDING as a citation, and the anti-rot rule ("no ledger entry
# names a surface a product skill now documents") called the line stale. The ledger only shrinks, so the
# deletion was one-way, onto a fact that was still exactly as true as when it was written.
#
# #692 fixed the matcher: a binding is not a citation, and within a skill that binds a name only a QUALIFIED
# use reaches the API. So the false credit is gone — and the line still does not come back, because #663
# meanwhile documented `Scene.describe` FOR REAL (`fs-gg-scene` cites `Scene.describe hud` in an F# fence).
# It is now documented rather than blessed-as-undocumented, and a ledger line would be stale for the honest
# reason. Restoring it — which #692 asked for, written before #663 landed — would red the gate.
vocabulary  arc
vocabulary  black
# #1168: `bareCite` credited this from `fs-gg-ui-widgets`' `GraphView.nodes [ "form"; "chart"; "grid" ]` —
# the node LABEL string "chart", not a citation of `Scene.chart`. Fixing the string-literal homonym
# un-credits it; the surface is a Scene shape constructor same as its neighbours, so it lists here rather
# than growing a paragraph nobody asked for.
vocabulary  chart
vocabulary  circleEvidence
vocabulary  classify
vocabulary  clipped
vocabulary  close
vocabulary  combine
vocabulary  cubicTo
vocabulary  ellipse
vocabulary  ellipseEvidence
vocabulary  fill
vocabulary  filledEllipse
vocabulary  fromRenderEvidence
vocabulary  lineTo
vocabulary  moveTo
vocabulary  picture
vocabulary  points
vocabulary  quadTo
vocabulary  rectangle
vocabulary  rectangleWithPaint
vocabulary  renderHash
vocabulary  renderPng
vocabulary  renderReadbackEvidence
vocabulary  rgb
vocabulary  rgba
vocabulary  segment
vocabulary  sizedText
vocabulary  textRun
vocabulary  transparent
vocabulary  unsupported
vocabulary  vertices
vocabulary  white
vocabulary  withAntialias
vocabulary  withBlendMode
vocabulary  withColorFilter
vocabulary  withColorSpace
vocabulary  withImageFilter
vocabulary  withMaskFilter
vocabulary  withMiter
vocabulary  withOpacity
vocabulary  withPathEffect
vocabulary  withShader
vocabulary  withStrokeCap
vocabulary  withStrokeJoin

# ---- SkiaViewer/SkiaViewer.fsi
vocabulary  captureDiagnostic
vocabulary  classifyWindowObservation
vocabulary  classifyWindowState
vocabulary  defaultWindowBehavior
tracked     desktopSessionDiagnostic
vocabulary  failureFromDiagnostic
vocabulary  fit
vocabulary  initEvidenceWorkflow
vocabulary  initRun
vocabulary  initWithWindowBehavior
vocabulary  present
vocabulary  runInteractiveViewerScriptWithAudio
vocabulary  runInteractiveViewerScriptWithWindowBehaviorAndAudio
vocabulary  runInteractiveViewerWithWindowBehavior
vocabulary  runInteractiveViewerWithWindowBehaviorAndAudio
vocabulary  runUntilFirstFrame
tracked     runtimeCapability
vocabulary  shouldCaptureDiagnostic
vocabulary  smoke
vocabulary  toLogicalPoint
vocabulary  updateEvidenceWorkflow
vocabulary  updateRun
tracked     validateWindowBehavior
vocabulary  validateWindowLaunchBehavior

# ---- Symbology/Symbology.fsi
vocabulary  badge
vocabulary  ring

# ---- Testing/Testing.fsi
vocabulary  buildValidationContractOutput
vocabulary  classifyDrift
vocabulary  parseScreenshotEvidenceRecord
vocabulary  selectVisualEvidence
vocabulary  statusText
vocabulary  validateDefaultInteractiveLaunch
vocabulary  validateScreenshotArtifact
vocabulary  validateScreenshotEvidence
vocabulary  validateVisualEvidenceCommandOutput
vocabulary  validateWindowDiagnostics
vocabulary  verifyGeneratedTests
vocabulary  verifyPackageResolution

# ---- carried by more than one signature file
# The ledger keys on the NAME, so these lines each cover every `.fsi` that declares the name, and no single
# `# ---- <file>` header above would be an honest home for them. FOUR names sat here, all credited to English
# prose ("validate your GridSpec", "a headless replay", "(diagnostics) refers to the local update") until #654
# required a citation.
#
# TWO REMAIN, and both are vocabulary — see below. The other two were `tracked`, and #663 WROTE them, so the
# ratchet took their lines: `diagnostics` — the query that hands you the list, as against the Diagnostics.fsi
# constructors, which stay vocabulary — is cited by fs-gg-elmish (Control/ControlRuntime/ControlsElmish) and by
# fs-gg-scene, the only one of the two that reaches a headless-scene or governed product. `validate` is cited by
# fs-gg-testing (the evidence/readiness entry points) and fs-gg-ui-widgets (the control-authoring ones).

# `measure` — RichText (text measurement) and Scene (path measurement). Peers `measureHorizontal` /
# `measureVertical` are vocabulary; these are the same kind of total query over a value you already hold.
vocabulary  measure

# `selected` — Attributes and Control. An attribute combinator, beside `enabled` / `readOnly` / `onSelected`.
vocabulary  selected

# ---- documented by OWNER-SOURCED skills retired under ADR-0063 (FS.GG.Rendering#965)
#
# These api-surface vals WERE named in this repo's frozen `--profile game` skill copies —
# `fs-gg-game-core` (Game.Core: fixed-step / RNG / pathfinding / spatial-grid / geometry / physics),
# `fs-gg-audio` (Audio.Core/Host/Elmish + the Controls audio hosts and the audio-playback viewer), and
# `fs-gg-persistence` (Canvas/Persistence save-slot surface). ADR-0063's 2026-07-21 amendment RETIRED
# those four copies from this provider: they are now OWNER-SOURCED from the `FS.GG.Game.Skills` package
# and delivered into the scaffolded product by the SDD materializer. The bodies that document these vals
# therefore still reach every game/sample-pack product — they are just no longer checked out HERE, so
# S-DOC (which reads only `template/product-skills/**`) can no longer see them. Their `.fsi` still carry
# the `// See skill:` pointer to the owning skill (ApiSurfaceMirrorTests M-PTR accepts an owner-sourced
# target), and FS.GG.Game's own gate verifies the body against the surface. The decision recorded here:
# the documentation is owner-sourced, not absent. If a Rendering-owned skill ever documents one of these,
# delete its line (S-DOC rule 2 will flag it); if the package retires a surface, its pin drops the .fsi
# and rule 3 flags the line. Both keep this section honest.
owner-sourced  addBodies
owner-sourced  addBody
owner-sourced  advance
owner-sourced  alpha
owner-sourced  astar
owner-sourced  bfs
owner-sourced  center
owner-sourced  checksum
owner-sourced  child
owner-sourced  clampVolume
owner-sourced  defaultMaxFrameTime
owner-sourced  deleteSlot
owner-sourced  drain
owner-sourced  drainWith
owner-sourced  duck
owner-sourced  emptyEvidence
owner-sourced  interpolate
owner-sourced  manifold
owner-sourced  maxVolume
owner-sourced  minVersion
owner-sourced  minVolume
owner-sourced  nextFloat
owner-sourced  pairs
owner-sourced  playMusic
owner-sourced  playSfx
owner-sourced  playSfx3D
owner-sourced  runInteractiveAppWithWindowBehaviorAndAudio
# #1168: `bareCite` credited this from `fs-gg-elmish`'s `DispatchHostCommand "save"` — a host-command
# PAYLOAD string, not a citation of `Persistence.save`. Fixing the string-literal homonym un-credits it;
# `Persistence.save` is the same owner-sourced save-slot surface as its neighbours below (retired under
# ADR-0063 alongside `saveEnvelope`/`deleteSlot`/`emptyEvidence`/`minVersion`), just never listed here
# because the homonym had been standing in for the decision.
owner-sourced  save
owner-sourced  saveEnvelope
owner-sourced  setBusVolume
owner-sourced  setMasterVolume
owner-sourced  stopMusic
