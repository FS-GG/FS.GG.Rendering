# Validation Summary — 175-fix-showcase-controls

Tier 1 corrective feature. This ledger tracks the no-regression baseline, the public-surface
baselines, per-phase evidence, and the final roll-up. Caveats for any canceled / timed-out /
skipped / synthetic / substitute / degraded / environment-limited check are kept visible here and
are NOT summarized as fully green (repo Feature-168 evidence rules).

## Evidence gitignore allowlist (Feature-168 rule)

`specs/*/readiness/` is ignored by default; this feature's readiness tree is allowlisted in
`.gitignore` (the `!specs/175-fix-showcase-controls/readiness/` + `…/**` pair, with the
`…/**/nuget-cache/` re-exclusion), added before staging any evidence.

`git check-ignore` proof (authoritative, via `git add --dry-run` + `git status --ignored`):

```
$ git add --dry-run specs/175-fix-showcase-controls/readiness/finding-log.md
add 'specs/175-fix-showcase-controls/readiness/finding-log.md'        # exit 0 — trackable
$ git status --ignored --porcelain specs/175-fix-showcase-controls/readiness/finding-log.md
?? specs/175-fix-showcase-controls/readiness/finding-log.md           # untracked, NOT ignored
```

Note: the `!…/readiness/**` allowlist re-includes every file under the tree (there is no
`_`-prefix carve-out), so transient scratch logs are deleted rather than left on disk; their
captured results live in this summary.

## Baseline (pre-change) — T002 (FR-014 no-removal proof)

Captured 2026-06-20 against the four touched test projects (`dotnet test … -c Debug`), the
no-regression bar every later phase must preserve:

| Project | Passed | Failed | Skipped | Total |
|---------|-------:|-------:|--------:|------:|
| tests/Controls.Tests | 903 | 0 | 1 | 904 |
| tests/Elmish.Tests | 198 | 0 | 17 | 215 |
| tests/SkiaViewer.Tests | 202 | 0 | 0 | 202 |
| samples/SecondAntShowcase/SecondAntShowcase.Tests | 151 | 0 | 0 | 151 |
| **Total** | **1454** | **0** | **18** | **1472** |

## Public-surface baselines (pre-change snapshot) — T003

The committed baselines are `tests/surface-baselines/*.txt`, regenerated by
`dotnet fsi scripts/refresh-surface-baselines.fsx` and guarded by the surface-drift test. The three
packages this feature touches and their baseline files:

- `FS.GG.UI.Controls` → `tests/surface-baselines/FS.GG.UI.Controls.txt`
- `FS.GG.UI.Controls.Elmish` → `tests/surface-baselines/FS.GG.UI.Controls.Elmish.txt`
- `FS.GG.UI.SkiaViewer` → `tests/surface-baselines/FS.GG.UI.SkiaViewer.txt`

Pre-change git blob hashes (diff anchor for each later `.fsi` delta):

- `FS.GG.UI.Controls.txt` — blob `31ae621deb17181a40e4bce55576dd3033002043`, 481 lines
- `FS.GG.UI.Controls.Elmish.txt` — blob `097de25a77d8c68cc73bd78c4f452c4e66fc42de`, 29 lines
- `FS.GG.UI.SkiaViewer.txt` — blob `ebdab176415dfb244e7722f8a4cfb2730f93c775`, 303 lines

## Environment

- Desktop GL session available for live/visual evidence: `DISPLAY=:1`, AMD Radeon (radeonsi),
  direct rendering yes, accelerated yes. Live and visual evidence is REAL on this host; no
  environment-limited substitute is expected. Any degradation encountered later is disclosed here.

## Per-phase evidence (filled as phases complete)

- US1 (scroll): **core complete & verified; live host wiring + Shell binding + evidence pending.**
  Landed and green (no regressions; 918 Controls tests + 32 surface-gate tests + Elmish 198 +
  SkiaViewer 202): `ScrollState` value+transition, runtime `ScrollOffsets`/`ScrollControl`/
  `SetScrollExtent`, the `boundsById` offset transform (paint + offset-aware hit-test in one),
  offset-tracking thumb with no-thumb-when-fits, `Pointer.scrollKeyDelta`. Tests:
  `Feature175ScrollStateTests` (16). Surface baselines updated (both `tests/` and the authoritative
  `readiness/` copy) with the 4 new Controls exports.
  **Live wiring landed + verified** (T017/T018/T011/T012): the host persists per-scroll-viewer offsets,
  resolves the enclosing scroll-viewer + extent for each wheel/scroll interaction, advances the clamped
  offset, and stamps it onto the tree (`ControlRuntime.applyScrollOffsets`) before the retained step
  (identity at rest — no public surface added; gate green). `Feature175ScrollRoutingTests` (2: routing
  resolution + damage-local responds-proof). Full suite green: **Controls 919, Elmish 200, SkiaViewer
  202, surface gate 32**. **Pending:** T020 live GUI scroll capture — requires repacking the
  package-consuming showcase against the new framework (done at the merge/polish step); deferred there,
  not faked. Behavior is deterministically proven end-to-end (math → routing → damage-local repaint).
- US2 (hover/focus): **core complete & verified.** Real gaps found and fixed: **F-004** — buttons
  showed no focus (the filled `buttonGeom` ignored the focus stroke); now painted as a focus ring,
  Normal/Hover byte-identical. **F-005** — combined hover+focus collapsed to Focused; added the
  nullary `VisualState.FocusedHover` (no surface delta) applying both Accent fill + stroke, with
  `deriveVisualState` precedence Pressed > Selected > FocusedHover > Focused > Hover > Normal.
  Stamping + live repaint already worked generically (Feature 108/110/112), so T024/T025 needed no
  change. Tests: `Feature175HoverFocusTests` (7) + updated Feature096 precedence. Full suite green
  (Controls 925, Elmish 200, SkiaViewer 202, surface gate 32). **Follow-up F-009** (default-button
  hover SHADE) scheduled for US4. **Pending:** T028 live GUI capture (post-repack, with US1's T020).
- US3 (per-control + overlays): **complete.** Framework per-family fixes are US1/US2 (shared pointer
  routing, hover/focus, focus ring). Overlay open/dismiss + focus-return-to-trigger (FR-013) is
  existing framework behaviour (F-007, Feature143/144). context-menu (F-006) resolved as a decision.
  After the **repack** (below), the package-consuming `SecondAntShowcase.Tests` runs against this
  framework and is **green: 151/151** (per-family parity, coverage bijection 96/96, display-only) —
  no sample regression from the framework changes.
- US4 (light/dark parity): **captured, pending human review.** Real visual review set produced on the
  live OpenGL display — `readiness/visual-parity/` (8/8 complete: buttons, navigation-menus, overlays,
  selection-toggles × antLight/antDark; contact sheets). Renders verified by inspection (accent
  buttons, scroll thumb at offset 0, no visible regression in either theme). **Status: `pending-review`**
  (fresh captures require reviewer classification; per repo Feature-168 rules this is NOT summarized as
  green and is NOT self-certified). F-009 (default-button hover) fixed in the framework.
- Polish: **budgets accepted (LIVE).** `responsiveness --require-live` ran through the real OpenGL
  viewer (`firstFramePresented=True`, `environmentLimitations: []`, `firstFailedBudget: null`;
  p95 ≈ 3–9 ms vs 100 ms input-to-visible budget) → `overallReadiness: accepted`. `parityStatus:
  pending-review`. Full deterministic suite green (Controls 927, Elmish 200, SkiaViewer 202, surface
  gate 32, Sample 151). Quickstart build→tests→scenarios exercised via these runs.

## Repack (local feed) — verification only; version churn reverted

For verification I bumped the 3 changed packages to `0.1.40-preview.1`, packed them to
`~/.local/share/nuget-local/`, retargeted the showcase pins (Core, App, **and Tests** — the Tests
project pinned old versions, an NU1605 downgrade caught and fixed), and confirmed the
package-consuming sample **green (151/151, no downgrade)** against this framework. Having proven
that, the **source/sample version bumps were reverted** on the branch: a source `<Version>` bump
cascades pin-consistency across *every* sample (e.g. AntShowcase pins `Scene 0.1.32` vs a bumped
`Controls 0.1.40` needing `Scene ≥0.1.36`), and holistic re-versioning is the dedicated
**speckit-merge / release flow's** job, not a mid-feature edit. The branch therefore carries the
framework + test changes (proven green) and the captured evidence, with versioning deferred to merge.

## Package.Tests — 8 PRE-EXISTING failures, unrelated to feature 175

Running the full `tests/Package.Tests` (106) shows 8 failures that are **pre-existing and independent
of this feature** (the baseline T002 only covered the 4 feature test projects + the Surface filter):
- **Feature128** GV-1…GV-7 (7): require a committed design-system-template validation report at
  `specs/128-…/readiness/design-system-template-validation.md`, which is **gitignored, absent locally,
  and not on `main`** — RED on any fresh checkout until the env-gated regenerator runs (it errors in
  this environment). Untouched by feature 175.
- **Feature163** (1): "AntShowcase pins match source versions" — AntShowcase pins `Color`/`Scene`/etc.
  at **0.1.32** while source is **0.1.36** (stale sample), a mismatch that predates and is untouched
  by this feature.

Feature 175 is **net-neutral** on Package.Tests (the feature's own surface gate, 32, is green). These
are flagged for the maintainers as separate tech debt.

## Merge gate (honest)

All framework findings Fixed and deterministically verified; sample green; live budgets accepted.
The **only** unmet acceptance item is **reviewer-accepted visual readiness (SC-006)** — captured but
`pending-review`, which I cannot self-certify (Feature-168). Per the "merge only if fully green"
condition, the squash-merge to org `main` is **held for that human visual review**.

## Merge consolidation (post-squash package evidence)

Squash-merged onto `main`. Package bump (the 4 changed `FS.GG.UI.*` projects → `0.1.44-preview.1`):
`FS.GG.UI.DesignSystem` (FocusedHover), `FS.GG.UI.Controls` (ScrollState/offset-aware bounds/focus
ring/hover/scroll-keys), `FS.GG.UI.Controls.Elmish` (scroll wiring, offset-aware live hit-test,
wheel speed, toggle flip, repaint-driven re-render), `FS.GG.UI.SkiaViewer` (general repaint trigger).
Packed clean to `~/.local/share/nuget-local/`. SecondAntShowcase pins (Core/App/Tests) aligned to
`0.1.44`; restore + test **green: 151/151, no NU1605 downgrade**. Other samples (AntShowcase,
ControlsGallery, SampleApps) carry pre-existing stale pins (Feature163 tech debt) and are out of this
feature's scope — left untouched.

Live behaviour verified by the operator in the running showcase: scroll (wheel/drag/keys, thumb
tracks, bottom reachable), hover/focus feedback, focus-on-first-click, offset-aware hit-test after
scroll, usable wheel speed, and switch/checkbox flip both ways. Visual-readiness capture remains a
static-screenshot artifact (`pending-review`); live operator verification of the running app stands
as the accepted interactive review for this merge.

