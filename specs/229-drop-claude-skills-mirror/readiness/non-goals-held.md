# Non-goals held (T022) — Feature 229

## Files touched (`git diff --name-only main...HEAD`, code files)

```
.template.config/template.json                          # remove all .claude/skills/ sources
.template.package/FS.GG.UI.Template.fsproj              # version 0.1.58-preview.1 -> 0.1.59-preview.1 (ONLY)
scripts/validate-lifecycle-template.fsx                # classifier + guard + workspace floor + spec-kit obs
tests/Package.Tests/Feature204LifecycleTemplateTests.fs # gate correction + universal .claude/skills/ guard
tests/Package.Tests/Feature219EmitFrameworkSkillsTests.fs # gate: .agents-only surface assertion
CLAUDE.md                                              # SPECKIT marker -> 229 plan
.specify/feature.json                                  # feature dir pointer
specs/229-drop-claude-skills-mirror/**                 # spec artifacts + readiness evidence
specs/204-.../readiness/lifecycle-template-validation.md # regenerated by the fsx (gitignored readiness)
```

## Invariants confirmed

- **No `src/**` file changed; no `.fsi` changed** — `git diff --name-only main HEAD -- 'src/**' '**/*.fsi'` empty (Tier 2 / FR-010).
- **No dependency change.** No `PackageReference` added.
- **Framework packages unchanged** — `<FsGgUiVersion>` stays `0.1.58-preview.1`; only the template package version bumped. Feature 209 coherence gate green (7/7).
- **`.codex/skills/` still never written** — `grep -c codex .template.config/template.json` = 0.
- **Feature 224/225 gates untouched and green**; `docs/reports/skills-parity.md` NOT regenerated (no skill file added/removed — only scaffold *placement* of existing skills changed).
- **Full baseline** (`baseline-after.md`): 21 projects, 21 green, 0 red — no regression vs the pre-fix baseline (both 21/21 green).
