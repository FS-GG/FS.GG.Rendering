# FS.GG.UI version coherence — verdict report

Regenerated by `scripts/validate-version-coherence.fsx`. The merge-blocking gate step
"Version coherence guard" re-derives this verdict on every PR.

The tag lines below are the tags this COMMIT owes — derived from the pin and the package
version, never from `git tag --list`. A committed artifact that recorded the observed tag set
would go stale the moment a release cut a tag, with no commit to blame (#514, class #517); that
the owed tags equal the observed ones is checked live, on every run, and is what `result:` says.

- feature: 209-version-staleness-guard
- result: pass
- provenance: verdict-core
- single-version-source: 0.19.0 (`template/base/Directory.Packages.props:9 <FsGgUiVersion>`, occurrences=1)
- snapshot-tag-for-pin: fs-gg-ui/v0.19.0
- published-members: 16 · bom-deps: 16 · template-consumed-pins: 14
- runtime-regex-resolves: true
- template-package-version: 0.19.2 (`.template.package/FS.GG.UI.Template.fsproj:9 <Version>`)
- release-tag-for-package: v0.19.2 · template-tag-for-package: fs-gg-ui-template/v0.19.2
- framework-pin-vs-package: 0.19.0 <= 0.19.2 = true
- resolved-members-at-version: pending-live (run FS_GG_RUN_VERSION_COHERENCE_SMOKE=1)

All lockstep conjuncts hold for the layers that ran.
