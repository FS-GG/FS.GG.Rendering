# Contract: Post-split Module Topology, Surface, Ordering & Oracle

**Feature**: 189-control-module-split | **Date**: 2026-06-22

This is the interface contract the implementation MUST satisfy. `FS.GG.UI.Controls` exposes its public
surface via `.fsi` files; the decomposition targets are internal, so the published contract is mostly a
**negative** one (no public drift) plus the **extended internal registry** and the **completeness
oracle**.

---

## C1 — Compile-order contract (`Controls.fsproj`)

Producers strictly precede consumers; no module references a later-compiled module (INV-ORDER).

```
… Types → Diagnostics → OverlayState → ControlKindRegistry(meta) → Accessibility
  → Internal/AttrKeys → Internal/Hashing → Shared/Numeric → Attributes → Composition
  → ControlPrimitives            (NEW: shared prelude, was Control.fs L124–625)
  → ChartGeometry                (NEW: chart *Geom + withPoints)
  → WidgetGeometry               (NEW: widget/layout/container *Geom)
  → SceneHash                    (NEW: SceneHasher visitor)
  → ContentRender                (NEW: faithfulContent + registry painter table)
  → LayoutEval                   (NEW: toLayout/evaluateLayout/evaluateLayoutIncremental)
  → NodeAssembly                 (NEW: paintLeaf/paintNode/renderScene)
  → Control                      (residual ControlInternals core + module Control + 30 tail modules)
  → Reconcile → Widget → Catalog → TextInput → ControlRuntime → … → RetainedRender → Inspection → …
```

- The relative order of `ContentRender`↔`NodeAssembly` and the painter-field-vs-separate-map shape are
  fixed **empirically** at the Foundational compile probe (research D5). The order above is the
  expected outcome; the probe is authoritative.
- **Each new `.fs` has a paired `.fsi`** added to the project in the same `<Compile>` adjacency
  (Constitution II); all new modules are `module internal`.

## C2 — Public surface contract (NEGATIVE)

- `readiness/surface-baselines/FS.GG.UI.Controls.txt` regenerated by
  `scripts/refresh-surface-baselines.fsx` MUST diff **empty** against the captured pre-refactor
  baseline. Target: empty. Version bump **iff** non-empty and reviewed (FR-010 / SC-006).
- The 30 public tail modules (`TextBlock`…`Overlay`) and `module Control` keep every `create`/`text`
  name and signature. `Control.fsi` (687 lines) is unchanged except where a reviewed non-empty diff is
  explicitly accepted.
- `tests/Package.Tests/SurfaceAreaTests.fs` ("FS.GG.UI.Controls baseline exports expected contract
  names") MUST stay green.

## C3 — Extended internal registry contract

- The registry gains a per-kind `Painter: Theme -> Rect -> Control<'msg> -> Scene list` (FR-005),
  **preferably as a sibling `painters: Map<string, Painter>`** rather than a field on `ControlKindEntry`:
  the entry is currently non-generic, and a generic painter field would force `ControlKindEntry<'msg>`
  across the registry `Map` and all metadata readers (genericity ripple). The painter **values** are
  bound in `ContentRender`. The field-on-entry form is used only if a non-generic painter shape keeps
  the record non-generic; the chosen form is the recorded FR-005 deviation.
- `faithfulContent: Theme -> Rect -> Control<'msg> -> Scene list` keeps its name/shape and now resolves
  via the registry painter (one table read; SC-003).
- Non-catalog runtime kind → the **same default** the pre-refactor `match | _ ->` produced (FR-007).

## C4 — Completeness-oracle contract (FR-007 / SC-007)

`tests/Controls.Tests/Feature183KindRegistryTests.fs` extended so a **catalog kind with no `Painter`
fails the test loudly**, in addition to the existing both-directions key-equality check. Machine-
enforced exhaustiveness — adding a kind is a one-site, oracle-checked change.

## C5 — Behavioral-equivalence contract (FR-003/004/009, SC-004/008)

- `hashScene` byte-identical for the scene corpus; `hashScene [] = emptySceneListFingerprint`
  byte-identical. Any legitimate reorder recorded in `readiness/golden-hash-review.md` with proof it
  breaks no `RetainedRender` picture-cache invariant.
- `evaluateLayout` `root`/`boundsById` byte-identical (INV-1).
- Every `*Geom` / painter / `renderScene` scene byte-identical where construction guarantees it.

## C6 — Fail-loud contract (FR-012)

`required`-attribute validation (`Control.fs:353`), unknown-kind fallback, and missing-painter
diagnostics all keep firing with the same diagnostics — no swallowed exceptions, no silent
degradation.

## C7 — No-new-dependency contract (FR-013)

No new project, NuGet package, or inter-project reference. `Controls.fsproj` changes are limited to
added `<Compile>` entries (and the consumer/baseline edits a non-empty surface diff would require).
